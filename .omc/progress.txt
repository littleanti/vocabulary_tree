[2026-05-17 init]
- M0~M7 구현 완료, 마일스톤별 커밋 8개 + docs(M7) 푸시됨.
- 이번 ralph 세션: Playwright headless로 핵심 사용자 시나리오 자동 검증.
- 환경: WSL2 Linux + Node 사용 가능, gh CLI ok.

[iteration 1]
- PRD: 7 stories (US-001~US-007), 카테고리 = 로드/시뮬/오버뷰/보스/대시보드/PWA/종합

[iteration 2 — Playwright 부팅]
- Chromium 시스템 deps(libnspr4) 누락 → sudo 불가
- Firefox도 libasound2t64 누락
- apt-get download 로 .deb 다운로드 → /tmp/usrlibs 에 dpkg-deb -x 추출
- LD_LIBRARY_PATH=/tmp/usrlibs/usr/lib/x86_64-linux-gnu BROWSER=firefox PLAYWRIGHT_SKIP_VALIDATE_HOST_REQUIREMENTS=1 로 Firefox headless 부팅 성공

[iteration 3 — 발견된 실버그 3건 + 검증 하니스 버그 2건]
실버그 (앱 코드):
1. sw.js fetch handler가 fonts.gstatic.com / esm.sh 등 cross-origin 요청을 wrap 하면서 NetworkError 발생 → 콘솔 에러
   → 수정: cross-origin은 SW 인터셉트하지 않고 브라우저 기본 fetch 사용. CACHE_NAME → vt-cache-v2.
2. tree/overview.js: createCamera 호출이 leafEntries const TDZ 안에서 placeLeaves(leafEntries) 호출 → ReferenceError 'leafEntries before initialization'
   → 수정: leafEntries를 createCamera 전으로 이동, minimap은 let으로 forward declare.
3. blocks/drag.js: setPointerCapture가 synthetic PointerEvent에서 throw 가능 → try/catch로 감쌈 (방어).

검증 하니스 버그 (verify.mjs 자체):
A. 누적 어휘 정규식 `\D+` 가 "누적 어휘4어휘"에서 매치 실패 (\D+는 1+ non-digit 요구) → `[^0-9]*` 로 수정
B. minimap의 contentSvgInnerHTML이 메인 SVG의 innerHTML을 복제하면서 id='ov-g'가 문서에 2개 존재 → 메인 SVG 내부로 querySelector 스코프 한정

[iteration 4 — 최종]
- 30 / 30 통과
- 콘솔 에러 0건, 네트워크 실패 0건
- 로그: tools/web-verify.log

학습:
- Playwright의 page.mouse는 Firefox 헤드리스에서 일부 드래그 시나리오에서 신뢰성이 떨어짐 — page.evaluate 안에서 PointerEvent를 dispatch 하는 게 deterministic.
- 시스템 라이브러리가 없을 때 apt-get download → dpkg-deb -x → LD_LIBRARY_PATH 가 sudo 없이 PW 브라우저 부팅하는 좋은 우회 방법.
- Service Worker가 cross-origin 자원을 적극 인터셉트하면 CORS / opaque-response 이슈로 콘솔 에러 발생; 정적 PWA에서는 same-origin만 인터셉트가 안전.
